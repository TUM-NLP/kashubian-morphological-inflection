# Transforming data into JSON Lines / tsv format compatible with ByT5
The output is stored in `./data`
- ```combine_language.ipynb```: optional combining languages, and turning SIGMORPHON data into JSON format
- `sentences.ipynb`: span corruption of Kashubian and Polish sentences and conversion to `.tsv` format